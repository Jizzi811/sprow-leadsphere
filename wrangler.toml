# Cloudflare Pages Konfiguration (Repo-Root)
# Cloudflare erkennt diese Datei automatisch beim Git-Deploy.
name = "sprow-leadsphere"
compatibility_date = "2026-07-21"

# Build-Konfiguration
pages_build_output_dir = "dist"

[build]
command = "cd SPROW-LeadSphere && npm install && npm run build"
